<%- include('partials/header')  -%>
<div class="container box p-6 has-background-light">
    <h1 class="title has-text-centered">
        Prosecutor's Office
    </h1>
    <h2 class="subtitle has-text-centered">
        Настройки
    </h2>
    <div>
        <div>
            <figure class="image is-128x128">
                <img class="is-rounded" src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>">
            </figure>
        </div>
        <hr>
        <div>
            <div>
                <div>
                    <h4><%= user.username %>#<%= user.discriminator %></h4>
                </div>
            </div>
            <div class="">
                <p class="text-muted">Уровень доступа: <b><%= user.level %></b></p>
                <p class="text-muted">Должность: <b><%= user.rank %></b></p>
                <p class="text-muted">Округ: <b><%= user.position %></b></p>
                <p class="text-muted">Никнейм: <b><%= user.nickname %></b></p>
                <p class="text-muted">Номер: <b><%= user.phone %></b></p>
            </div>
        </div>
    </div>
    <hr>
    <form action="/profile/settings" method="POST">
        <div class="field">
            <label class="label">Никнейм</label>
            <div class="control">
                <input class="input" type="text"
                       placeholder="Enter your nickname"
                       name="nickname"
                />
            </div>
        </div>

        <div class="field">
            <label class="label">Номер</label>
            <div class="control">
                <input
                        class="input is-grouped"
                        type="number"
                        placeholder="Enter your phone number"
                        name="phone"
                />
            </div>
        </div>
        <div class="field is-grouped">
            <div class="control">
                <button type="submit" class="button is-success">
                    Сохранить
                </button>
            </div>
            <div class="control">
                <a class="button is-link is-light" href="/profile">Отмена</a>
            </div>
        </div>
    </form>
</div>
<%- include('partials/footer')  -%>